# Application
APP_NAME=NutriProfile API
APP_VERSION=0.1.0
DEBUG=false
ENVIRONMENT=production

# Server (Render utilise la variable PORT automatiquement)
HOST=0.0.0.0
PORT=10000

# Database (SQLite pour Render - pas besoin de PostgreSQL)
DATABASE_URL=sqlite+aiosqlite:///./data/nutriprofile.db

# Security - IMPORTANT: Generez de nouvelles cles pour la production!
# Generez avec: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_IN_PRODUCTION
REFRESH_SECRET_KEY=CHANGE_ME_IN_PRODUCTION
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Hugging Face - Votre token HuggingFace
HUGGINGFACE_TOKEN=hf_your_token_here

# CORS - URLs autorisees (ajoutez votre URL Cloudflare Pages)
CORS_ORIGINS=["https://nutriprofile.pages.dev","https://nutriprofile-frontend.pages.dev"]
